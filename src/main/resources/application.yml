spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
scheduler:
  default-interval: ${SCHEDULER_INTERVAL:30000}
request:
  timeout: ${REQUEST_TIMEOUT:2000}
